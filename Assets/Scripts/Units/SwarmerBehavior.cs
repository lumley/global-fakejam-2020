
using Fakejam;
using UnityEngine;

[CreateAssetMenu(fileName = nameof(SwarmerBehavior), order = 0, menuName = AssetMenuConstants.ScriptableObjectsMenu + nameof(SwarmerBehavior))]
public class SwarmerBehavior : SquadBehavior
{
}
